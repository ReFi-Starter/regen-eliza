{
  "buildCommand": "cd website && pnpm install && pnpm build",
  "installCommand": "echo 'Dependencies installed in build command'",
  "outputDirectory": "website/.next",
  "framework": "nextjs",
  "env": {
    "ENABLE_ROOT_PATH_BUILD_CACHE": "0",
    "VERCEL_FORCE_NO_BUILD_CACHE": "1"
  }
}
